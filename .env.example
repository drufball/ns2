# Required when [issues.github] backend is active.
# Generate at https://github.com/settings/tokens with repo scope.
GITHUB_TOKEN=

# Required when [issues.github] backend is active and webhook validation enabled.
NS2_GITHUB_WEBHOOK_SECRET=
