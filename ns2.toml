[worktrees]
# base directory where ns2 creates git worktrees (default: ~/.ns2/<repo-name>/worktrees/)
# path = "~/.ns2/my-project/worktrees/"

[server]
url = "http://127.0.0.1:9876"

[issues]
# backend = "sqlite"  # default — no external config required
# backend = "github"  # GitHub Issues; requires GITHUB_TOKEN + NS2_GITHUB_WEBHOOK_SECRET in .env
# backend = "shell"   # user-defined script

[issues.github]
owner = "drufball"
repo  = "ns2"

# [issues.shell]
# command = ".ns2/backends/my-backend.sh"
