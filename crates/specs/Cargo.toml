[package]
name = "specs"
version = "0.1.0"
edition = "2021"

[dependencies]
workspace = { path = "../workspace" }
chrono = { workspace = true }
glob = "0.3"
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
